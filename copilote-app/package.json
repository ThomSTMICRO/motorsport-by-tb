{
  "name": "copilote-app",
  "version": "0.1.0",
  "private": true,
  "description": "Copilote automobile conversationnel — coût d'import de véhicule (carte grise + malus). Zéro dépendance npm.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js"
  }
}
